# libx265 编码指南：Preset、CRF、内容类型与输出结果

> 本文档基于 51 次实际编码测试（3 段 OPPO 手机视频 × 4 种 preset × 多档 CRF），
> 所有数据均为实测值，非理论推算。测试环境：MacBook Pro 2015, i7-4980HQ, macOS 12.7.6, ffmpeg 5.1.2。

---

## 1. 核心概念

### CRF (Constant Rate Factor)
CRF 是 x265 的质量目标参数，范围 0-51：
- **数字越小** → 质量越高 → 文件越大
- **数字越大** → 质量越低 → 文件越小
- x265 默认 CRF 28（约等于 x264 的 CRF 23）

### Preset（编码预设）
Preset 控制编码器花多少时间分析每一帧：
- **越慢的 preset** → 分析越深入 → 在相同 CRF 下保留更多细节
- **越快的 preset** → 跳过大部分分析 → 在相同 CRF 下丢弃更多细节

---

## 2. 关键发现：x265 与 x264 的 preset 行为相反

这是最重要的知识点，也是很多用户困惑的根源。

### x264（H.264）的行为
相同 CRF 下：
```
越慢的 preset → 文件越小（压缩效率更高）→ 质量相同或略好
```
这是直觉行为：慢 = 更高效 = 更小文件。

### x265（H.265）的行为 —— 相反！
相同 CRF 下：
```
越慢的 preset → 文件越大（保留更多细节）→ 质量略好
越快的 preset → 文件越小（丢弃更多细节）→ 质量略差
```

### 实测数据验证

以 VID20260727155148（源码率 16.27 Mbps）为例：

| Preset | CRF 16 码率 | CRF 16 SSIM | CRF 20 码率 | CRF 20 SSIM |
|--------|------------|-------------|------------|-------------|
| slow | 17.0 Mbps | 0.9916 | 8.2 Mbps | 0.9872 |
| ultrafast | 10.7 Mbps | 0.9854 | 5.2 Mbps | 0.9800 |
| **slow/ultrafast 比** | **1.59×** | +0.0062 | **1.58×** | +0.0072 |

> **结论**：在相同 CRF 下，slow 比 ultrafast 产生 **约 60% 更大** 的文件，
> 但 SSIM 仅高 **0.004-0.007**（肉眼几乎不可察觉）。

这一行为在所有 3 段测试视频中一致：
- 简单内容：slow/ultrafast 码率比 = 1.58-1.64×
- 复杂内容：slow/ultrafast 码率比 = 2.32×（slow 保留更多噪声细节）

### 为什么会这样？
x265 的慢 preset 开启了更多分析工具（lookahead、subme、rd-level 等），这些工具
让编码器更准确地判断"哪些细节值得保留"。在相同 CRF（即相同量化目标）下，
慢 preset 会选择保留更多高频细节，从而使用更多比特。

这与 x264 不同，因为 x264 的 CRF 机制和工具集设计与 x265 有本质区别。
Doom9 论坛和 x265 官方文档均确认了这一行为。

---

## 3. CRF 与码率的关系

### 不是简单的 0.85 因子

社区常引用"CRF 每增加 1，码率乘以约 0.85"的说法。实测数据显示这只是一个近似值，
实际因子随 preset 和内容变化：

| Preset | 实测每+1 CRF 码率因子 | 范围 |
|--------|---------------------|------|
| ultrafast | 0.835 - 0.879 | 平均约 0.86 |
| slow | 0.813 - 0.867 | 平均约 0.84 |

> **注意**：因子不是恒定的。对于复杂内容，slow preset 的衰减因子可低至 0.81
> （即每 +1 CRF 码率减少约 19%），而非 15%。

### 无法用公式准确预估

我们尝试了以下方法，均无法可靠预估 CRF：
1. **公式法**：`CRF = 20 + ln(bitrate/20) / ln(0.85)` — 因子和参考码率都随内容变化  
2. **探针法**：编码短片段估算 — 短片段不具代表性（同一视频不同位置 CRF 估算相差 3-9）  
3. **ab-av1**：VMAF 二元搜索 — x265 slow preset 极慢（0.8 fps），不实用  

社区共识（slhck CRF Guide、Doom9 论坛）：**CRF → 码率是内容相关的，
不编码就无法准确知道输出码率**。YouTube/Netflix 用神经网络预测也仅有约 80% 准确率。

---

## 4. 内容类型对结果的影响

三段测试视频均为 OPPO 手机拍摄（H.264 1080p, ~16.3-16.4 Mbps），但内容差异导致结果显著不同：

| 视频 | 内容特征 | CRF 16 slow 码率 | 占源比例 | SSIM |
|------|---------|-----------------|---------|------|
| VID20260727155148 | 干净/光线好 | 17.0 Mbps | **104%** | 0.9916 |
| VID20260723163245 | 中等复杂度 | 14.4 Mbps | 88% | 0.9898 |
| VID20260727152833 | 复杂/噪声多 | 11.9 Mbps | 72% | 0.9576 |

> **关键发现**：同样是 CRF 16 + slow，干净内容可能输出超过源文件（104%），
> 而复杂内容仅占源文件的 72%。**不存在一个"万能 CRF"能保证所有视频输出都小于源文件。**

### 内容类型与 preset 选择

| 内容类型 | slow 的优势 | 推荐 preset |
|---------|------------|-------------|
| 干净/光线好（白天户外） | SSIM +0.004-0.007，肉眼可察觉更好 | slow |
| 中等复杂度（室内/街景） | SSIM +0.003-0.005，略有优势 | slow 或 fast |
| 复杂/噪声多（夜景/暗光） | SSIM ±0.001，几乎无差异 | ultrafast（更快且差距小） |

---

## 5. 匹配码率下的质量对比

当 slow 和 ultrafast 输出相近码率时，slow 的 SSIM 略高：

| 目标码率 | slow (CRF) | SSIM | ultrafast (CRF) | SSIM | ΔSSIM |
|---------|-----------|------|-----------------|------|-------|
| ~15 Mbps | CRF 16 | 0.9916 | CRF 12 | 0.9897 | +0.0019 |
| ~10 Mbps | CRF 18 | 0.9895 | CRF 16 | 0.9854 | +0.0041 |
| ~7 Mbps | CRF 20 | 0.9872 | CRF 18 | 0.9828 | +0.0044 |
| ~5 Mbps | CRF 22 | 0.9850 | CRF 20 | 0.9800 | +0.0049 |

> **结论**：在相同码率下，slow 的质量优势约 +0.004 SSIM。
> 对于复杂内容（夜景），优势缩小至 ±0.001，几乎不可察觉。

---

## 6. 编码速度

以 16.5 秒视频为例（MacBook Pro 2015, i7-4980HQ）：

| Preset | 耗时 | 相对实时倍数 | 相对 ultrafast |
|--------|------|------------|---------------|
| ultrafast | ~170s | 10× | 1× |
| veryfast | ~220s | 13× | 1.3× |
| fast | ~260s | 16× | 1.5× |
| slow | ~510s | 31× | 3.0× |

> **结论**：slow 比 ultrafast 慢约 3 倍。对于批量处理大量视频，这是显著的时间成本。

---

## 7. 实用建议

### 如何选择 CRF

| CRF 范围 | 质量描述 | 适用场景 |
|---------|---------|---------|
| 14-16 | 视觉无损 | 收藏归档 |
| 18-20 | 极高画质 | 日常使用 |
| 22-24 | 高画质 | 体积优先 |
| 26-28 | 中等画质 | 预览/草稿 |

### 如何选择 Preset

| 场景 | 推荐 Preset | 理由 |
|------|------------|------|
| 收藏归档，时间充裕 | slow | 质量最优，相同码率下 SSIM 略高 |
| 日常使用，平衡速度与质量 | fast 或 medium | 速度比 slow 快 2×，质量差距 <0.005 SSIM |
| 快速处理大量视频 | ultrafast | 最快，配合低 CRF（14-16）可接近 slow CRF 18 的效果 |
| 复杂/夜景内容 | ultrafast | slow 在复杂内容上优势消失（±0.001 SSIM），不值得 3× 时间 |

### 保证输出小于源文件的经验值

基于 ~16 Mbps OPPO 手机视频的实测数据：

| Preset | 安全 CRF（保证输出 < 源） | 预期输出比例 | 预期 SSIM |
|--------|------------------------|------------|----------|
| slow | 18 | 60-80% | 0.988-0.990 |
| fast | 16 | 70-85% | 0.987-0.989 |
| ultrafast | 14 | 60-95% | 0.985-0.988 |

> **注意**：这些值基于 OPPO 手机 H.264 源视频（~16 Mbps, 1080p）。
> 对于其他类型的源视频（如蓝光、4K 等），安全 CRF 值可能不同。
> 建议先用一小段视频测试，确认效果后再批量处理。

---

## 8. 测试数据来源

- 测试视频：3 段 OPPO 手机录制视频（H.264, 1080p, ~16.3-16.4 Mbps, yuv420p 8-bit）
- 编码器：libx265 (ffmpeg 5.1.2)
- 测试矩阵：3 视频 × (ultrafast 7 CRF + veryfast 2 + fast 2 + slow 6) = 51 次编码
- 原始测试数据为内部资料，未随仓库发布

### 局限性
1. 测试仅覆盖 OPPO 手机 H.264 源视频，其他源类型（蓝光、4K、动画等）可能表现不同
2. SSIM 是客观指标，不完全等同于主观视觉质量
3. 测试在 Intel i7 CPU 上进行，ARM/Apple Silicon 上可能有不同的 preset 速度比
4. 未测试 10-bit 编码，10-bit 可能因减少色带而在相同 CRF 下产生不同码率
